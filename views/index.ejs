<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moon Day</title>
    <link rel="stylesheet" href="/styles/main.css"/>
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <div class="navbar-icons"> 
                <a class="navbar-previous" href="/previous">
                    <img src="images/caret-left.svg" alt="caret-left" width="30" height="30" class="navbar-icon">
                </a>
                <a class="navbar-next" href="#">
                    <img src="images/caret-right.svg" alt="caret-right" width="30" height="30" class="navbar-icon">
                </a>
            </div>

            <div>
                <button>Eng</button>
                <button>Ru</button>
            </div>
            
            <a class="navbar-calendar" href="#">
                <img src="images/calendar.svg" alt="calendar icon" width="30" height="30" class="navbar-icon">
            </a>
          
        </div>
    </nav>

    <div class="flex-container">
      <div class="card today">
        <h1><%= moonDayName %></h1>
        <h2><%= moonDay %> Lunar Day</h2>
        <% if (moonTime == 0) { %>
        <h3></h3>   
        <% } else {%>    
        <h3>started at <%= moonTime%></h3>
        <% } %>
        <% const imagePath = `images/IMG_${moonDay}.jpg`%>
        <img class="day-image" src = <%=imagePath%> />
        <h3><%= daySummary %></h3>
        <p><strong>Важно в этот день: </strong> <%= todo %> </p>
        <p><strong>Не стоит в этот день:</strong> <%= avoid %></p>
        <p><%= fullDescription %></p>
      </div>

      <div class="card calendar">
        <p> here should be table with calendar</p>
      </div>
    </body>
</body>
</html>